
myapp.run(function($rootScope) {
  //Code that would be run whenever use change URL link
  $rootScope.$on('$routeChangeStart', function(scope, next, current){
  		
        //perform logging functionalities or cross cutting concern     
        

   });
});